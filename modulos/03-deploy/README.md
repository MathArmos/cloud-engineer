# Seção 3 — Deploying and implementing a cloud solution

> **Domínio 3 da prova ACE.** Botar a mão na massa: criar e configurar os recursos via `gcloud`, console ou IaC.

---

## Subseções

### 3.1 — Deploying and implementing Compute Engine resources
**Workbook ACE — Diagnostic Questions:** 01, 02

**Tópicos principais:**
- Criação de instâncias GCE (machine types, images, disks)
- **Cloud SQL como alternativa a VMs** quando o objetivo é apenas hospedar DB
- **Managed Instance Groups (MIGs)** + instance templates
- **Rolling updates de SO:**
  - **Opportunistic** — só atualiza quando há outra operação (poupa recursos)
  - **Proactive** — força atualização imediata

**Arquivos documentados:**
- [3.1-compute-engine.md](3.1-compute-engine.md) — overview do módulo Compute Engine (Essential GCP Infra Foundation): o que é CE, anatomia de uma instância (5 dimensões), famílias de machine type, imagem ≠ disco ≠ instância, bloco "VM access and lifecycle" completo (acesso SSH/RDP, estados RUNNING/STOPPED/SUSPENDED/REPAIRING, reset, live migration, availability policy, shutdown timing 90s vs 30s preemptible, OS patch management, o que pode/não pode em TERMINATED), bloco "Compute Engine Pricing" completo (cobrança 1min mínimo + segundos, resource-based pricing, SUD até 30% automático, CUD 1/3 anos até 57%/70%, Preemptible vs Spot VMs, VM sizing recommendations, Free Tier). **Lab documentado:** Creating Virtual Machines (3 tasks — utility VM sem IP externo, Windows VM com RDP, custom machine type 2 vCPU + 4 GB com Debian).

---

### 3.2 — Deploying and Implementing GKE resources
**Workbook ACE — Diagnostic Questions:** 03

**Tópicos principais:**
- **Tipos de cluster:**
  - **Standard** vs **Autopilot** (Autopilot = Google gerencia nodes)
  - **Zonal** (1 zona) vs **Regional** (multi-zona, alta disponibilidade)
  - **Private** vs **Public**
- Node pools (separação de workloads)
- Image types (Container-Optimized OS vs Ubuntu)

**Arquivos documentados:**
- [3.2-gke.md](3.2-gke.md) — Autopilot vs Standard, Zonal vs Regional, VPC-native, kubectl básico. DQ 03 resolvida. **Curso "Getting Started with Google Kubernetes Engine":** bloco Module 1 (Introduction to Google Cloud) documentado em 6 sub-blocos — (1) 5 traços do cloud computing (NIST-style), (2) comparativo das 5 opções de compute do GCP com lente "containers" (CE/GKE/AE/Cloud Run/Cloud Run functions, incluindo cap de VM 416 vCPUs/12+TB, Persistent Disks até 257 TB, Knative em Cloud Run, free tier perpétuo em CRf), (3) infra global (40% do tráfego mundial, 100+ edge POPs, 7 macro-regiões, 41 regions/124 zones à época do curso), (4) hierarquia de recursos + IAM + Shared Responsibility Model ("if you configure or store it, you're responsible"), (5) billing no nível Project + 4 ferramentas de controle (Budget/Alert/Reports/Quota) + 2 tipos de quota (rate × allocation), (6) 4 formas de interagir com GCP (Console/SDK+Cloud Shell/APIs/Mobile app). Quiz Module 1 (2026-05-17) — 3 ✅ + 1 ⚠️ (Q4 hierarchy pegadinha, flashcard registrado). **Bloco Module 2 (Introduction to Containers and Kubernetes)** documentado em 3 sub-blocos: (7) Vídeo "What is a container?" — escada deploy físico → VM → container, problema multi-app em VM (starvation, dep hell), por que container venceu VM-por-app (kernel compartilhado vs kernels duplicados), definição user-space, 3 benefícios canônicos (code-centric, kernel-reliability, microservices); (8) Vídeo "Containers and container images" — image vs container (receita × bolo), 4 tecnologias Linux (process / namespace=visão / cgroup=consumo / union FS), Linux ≠ K8s namespace, anatomia de layers read-only + container layer efêmera, Dockerfile (FROM/COPY/RUN/CMD) com best practice de ordenação (estável topo, volátil base), multi-stage build, Artifact Registry (pkg.dev) substituindo Container Registry, Cloud Build (build steps em containers Docker, integrado com Source Repos/GitHub/Bitbucket, deploy multi-destino); (9) Vídeo "What is Kubernetes?" — a dor que K8s resolve (descoberta + scheduling + lifecycle em escala), definição canônica (open-source platform para workloads containerizados, 3 capacidades-âncora: orquestrar, escalar microservices, rollout/rollback), arquitetura control plane + nodes (no GKE control plane é gerenciado pelo Google), **armadilha de terminologia "node"** (K8s node = computing instance; GCP node = VM; no GKE coincidem), **declarativo × imperativo** (declarativo = describe estado desejado, auto-healing, estado documentado; imperativo só pra fixes rápidos/exploração), 4 workload types (Deployment/StatefulSet/Job-CronJob/DaemonSet), 3 autoscalers (HPA réplicas / VPA tamanho do pod / Cluster Autoscaler nodes), resource requests (mínimo, usado pelo scheduler) × limits (máximo, runtime mata se excede), CRDs como mecanismo de extensão, portabilidade no-vendor-lock-in (mesmo YAML em GKE/EKS/AKS/on-prem). Tabela consolidada com 14 armadilhas de prova do Module 2. **Sub-bloco 10 do Module 2** (transição → Module 3): vídeo "What is GKE?" — definição operacional de GKE managed K8s (COS, auto-upgrade, node auto-repair, cluster autoscaler), 4 features automáticas com armadilhas (auto-upgrade vs node auto-repair vs cluster autoscaler vs HPA), intro Autopilot (cross-ref pra seção detalhada), 5 integrações nativas (Cloud Build/Artifact Registry/IAM/Cloud Observability/VPC). **Quiz Module 2 (2026-05-18)** — 3 questões, 3 ✅ após reconsideração: Q1 (declarative inicialmente "Virtualization" → flashcard), Q2 (nodes inicialmente "Containers" → flashcard), Q3 (layered filesystem resolvida após explicação). **Bloco Module 3 (Kubernetes Architecture)** em 12 sub-blocos (2026-05-18): (1) 2 conceitos centrais (object model + declarative management, recap aprofundado), (2) anatomia de Object com spec/status (você escreve spec; control plane preenche status — pegadinha), (3) Watch Loop com diagrama de fluxo + imperativo vs declarativo, (4) Pod como building block (1 IP único, network namespace compartilhado, localhost intra-pod, volumes compartilháveis, diagrama de pod com 2 containers), (5) anatomia control plane vs nodes (hub-and-spoke pelo apiserver, diagrama ASCII), (6) **5 componentes do control plane** detalhados — kube-apiserver (auth+authz+admission, única porta), etcd (key-value Raft, só apiserver toca), kube-scheduler (decide node mas NÃO lança pod, considera recursos/constraints/affinity/anti-affinity/taints+tolerations), kube-controller-manager (workload controllers Deployment/ReplicaSet/StatefulSet/DaemonSet + system Node Controller + outros Job/CronJob), kube-cloud-manager (LBs e PDs no GCP), (7) **3 componentes do node** — kubelet (agente, executa probes liveness/readiness/startup), container runtime (containerd no GKE, Docker deprecated em 1.24), kube-proxy (iptables, regras de Service), (8) GKE como abstração — comparativo self-managed vs GKE em 6 componentes, quando Standard ainda faz sentido (DaemonSet privilegiado, GPU/TPU, hostPath/hostNetwork), (9) 13 pegadinhas consolidadas, (10) mnemônicos ASEC-C / KRP / "Scheduler nomeia, Kubelet executa" / "Tudo pelo APIserver", **(11) Vídeo "Autopilot vs Standard" deep dive** — 4 pilares do Autopilot (Production-optimized: machine type auto-definido por workload, battle-tested best practices; Strong security posture: locked-down nodes sem SSH, Google securiza infra; Operational efficiency: monitoring de control plane + worker nodes + core K8s components, **pod-scheduling SLA contratual**, update windows configuráveis; Pricing: pay-per-pod em vez de pay-per-node), 4 restrições do Autopilot (sem SSH/login no node, sem `privileged`/`allowPrivilegeEscalation`/`hostNetwork`/`hostPID`/`hostIPC`/`hostPath`, limitações em node affinity/taints custom, **todos os pods forçados a QoS Guaranteed** com requests==limits — conceito-base das 3 QoS classes do K8s explicado: Guaranteed/Burstable/BestEffort e comportamento sob eviction), decision tree (na dúvida → Autopilot, recomendação default do Google; Standard só com motivo específico como GPU/TPU rara, OS ≠ COS, hostPath, privileged DaemonSet), tabela de 15 pegadinhas + mnemônicos PSOP (4 pilares) e NPGH (4 restrições). Cross-reference adicionado na seção `## Autopilot vs Standard` apontando pro Sub-bloco 11 como material canônico. **(12) Vídeo "Kubernetes Object Management"** (encerra o Module 3) — **identidade tripla N-U-L** (Name humano: até 253 chars, letras/números/hífen/ponto, único por kind+namespace, reusável após delete; UID auto-gerado, único na vida inteira do cluster; Labels key-value pra agrupar e selecionar), anatomia do **manifest YAML** com 4 campos obrigatórios **AKMS** (`apiVersion`/`kind`/`metadata`/`spec`) + apiVersion por kind (`v1` core, `apps/v1` Deployment, `batch/v1` Job, `networking.k8s.io/v1` Ingress), exemplo de Pod nginx mínimo dissecado, best practices (objetos relacionados no mesmo YAML separados por `---`, version control com Cloud Source Repositories ou GitHub, tag específica em vez de `:latest`, `kubectl apply` idempotente vs `kubectl create`), **label selectors via kubectl** (`-l app=nginx` equality, `-l 'app in (x,y)'` set-based, vírgula = AND implícito), por que **Pod solto é armadilha** (ephemeral, não se auto-cura, não foi feito pra rodar pra sempre) e a solução com **Deployment**, **árvore Deployment → ReplicaSet → Pod** explicada (por que a indireção: rolling updates criam novo RS, rollback re-escala RS antigo) + fluxo end-to-end de 13 passos (kubectl → apiserver → etcd → Deployment Controller → ReplicaSet → Scheduler → kubelet → runtime), 4 tipos de controller recapturados com critério de escolha (Deployment stateless, StatefulSet stateful, DaemonSet per-node, Job/CronJob finito), elementos do `spec` do Deployment (`replicas`, `template.spec.containers`, `template.spec.volumes`) + armadilha clássica `selector.matchLabels` tem que casar com `template.metadata.labels` (senão pods órfãos), tabela de 16 pegadinhas + mnemônicos N-U-L, AKMS, "Pod = cattle não pet", "Deployment → ReplicaSet → Pod", "apply idempotente, create uma vez". **Quiz Module 3 (2026-05-18)** — 1 ✅ + 2 ⚠️: Q1 errou marcando "Pods soltos" em vez de Deployment (armadilha Pod = pet — flashcard); Q2 errou marcando "máquinas baseadas em workload" como Standard (na verdade é Pilar 1 do **Autopilot** — flashcard com regra de desempate: automático/hands-off/pay-per-pod → Autopilot; SSH/privileged/hostPath/Ubuntu/GPU → Standard); Q3 ✅ "mesmo Pod" para latência mínima (Sub-bloco 4). **Bloco Module 4 (Kubernetes Operations)** iniciado em 2 sub-blocos (2026-05-18): (1) **Intro do módulo** — overview do que vem (kubectl, configuração, introspection, troubleshooting via introspection, lab de deploy GKE via Cloud Shell); (2) **Vídeo "The kubectl command" deep dive** — definição (CLI que transforma comandos em chamadas HTTPS pra kube-apiserver, **não fala direto com etcd**), fluxo end-to-end de 8 passos (CLI → ~/.kube/config → HTTPS → apiserver → etcd → resposta → render), anatomia do `~/.kube/config` (clusters + users + contexts + current-context, YAML em `$HOME/.kube/config`), `kubectl config view` (meta-config do kubectl ≠ config dos objetos do cluster — pegadinha), credenciais via **`gcloud container clusters get-credentials`** (one-time per cluster no Cloud Shell, idempotente, **adiciona** ao config em vez de substituir, Cloud Shell já tem ambos pré-instalados), **divisão crítica `gcloud` vs `kubectl`** (gcloud cria/redimensiona/destrói cluster GKE; kubectl gerencia apenas conteúdo dentro do cluster — pegadinha clássica), sintaxe de 4 partes **COMMAND-TYPE-NAME-FLAGS** com tabelas de comandos canônicos (get/describe/logs/exec/apply/rollout/scale), formas curtas dos types (po/deploy/svc/rs/cm/ns/no/ing/pv/pvc), flags essenciais (`-o yaml/json/wide/name`, `-n/-A`, `-l`, `--kubeconfig`, `--context`, `-f`, `-w`), exemplos práticos (`get pod -o yaml` pra exportar, `-o wide` pra ver em qual node, `--context` pra switchar cluster sem mudar default), tabela de 14 pegadinhas + mnemônicos ("kubectl = cliente HTTPS da apiserver", "gcloud cria o cluster, kubectl vive dentro", "~/.kube/config = caderno de endereços", "4 partes: COMMAND TYPE NAME FLAGS", "-o yaml = exportar / -o wide = ver node", "get-credentials adiciona, não substitui", "kubectl não pilota infra GCP"). **(3) Vídeo "Introspection"** — debugging com 4 comandos (**GDLE**: Get/Describe/Logs/Exec — ordem da menor invasão pra maior); **6 Pod Phases** com diagrama de ciclo de vida (**Pending** = aceito mas não rodando, scheduler decidindo ou image pulling; **Running** = anexado em node + containers criados; **Succeeded** = todos containers terminaram OK e não reiniciam; **Failed** = container terminou com erro e não reinicia; **Unknown** = control plane perdeu contato com kubelet; **CrashLoopBackOff** = morreu pelo menos 1x e está em backoff exponencial entre tentativas — erro mais comum em produção, **não** significa "rodando"); causas típicas de CLBO (env var faltando, image errada, liveness agressivo, OOMKilled, ConfigMap/Secret inexistente). **`kubectl get`** com colunas READY/STATUS/RESTARTS/AGE como sinais de problema. **`kubectl describe pod`** = canivete suíço, 6 seções (Metadata, Status, Containers, Conditions, Volumes, **Events ⭐**); Events com Reasons (Scheduled, Pulling, FailedScheduling, ImagePullBackOff, OOMKilled, Unhealthy, FailedMount, BackOff); distinção **Pod Phase × Container State** (Container State = Waiting/Running/Terminated com sub-info de Reason e Exit Code, incluindo Last State pra diagnosticar). **`kubectl logs`** mecânica (containerd captura stdout/stderr → kubelet expõe → apiserver → kubectl), flag salva-vidas **`--previous`** pra ler logs do container morto em CrashLoopBackOff, `-c CONTAINER` em pod multi-container, `-f`/`--tail`/`--since`. **`kubectl exec`** mecânica (apiserver → kubelet → runtime → PID namespace do container), `-it` (i=stdin, t=TTY) — sem o par, shell sai imediatamente; anti-pattern: instalar pacotes/editar configs via exec (writable layer ephemeral, novo pod vem sem); citação canônica do vídeo: "interactive shell investiga, manifest corrige". **Workflow de troubleshooting** (get → describe → logs --previous → exec --debug → corrigir no manifest, redeploy) com tabela de 6 cenários típicos (Pending insufficient cpu, ImagePullBackOff 401, CLBO stack trace via --previous, Running mas não responde, OOMKilled recorrente, FailedMount). Tabela de 15 pegadinhas + mnemônicos GDLE (4 ferramentas), PR-SF-UC (6 phases), "Events = ouro da depuração", "CLBO → logs --previous", "OOMKilled mora em describe não em logs", "Exit 137 = SIGKILL = OOM/eviction". **Lab "Getting Started with Google Kubernetes Engine" (Qwiklabs, 2026-05-19)** documentado end-to-end na seção `## Labs`: setup com variáveis `my_region`/`my_cluster`, **Task 1** criar cluster Autopilot regional (`gcloud container clusters create-auto ... --region`, com anatomia de cada token + por que region obrigatório + cenário real Terraform), **Task 2** conectar kubectl (`get-credentials` cria/atualiza `~/.kube/config` com auth-provider gcp, pegadinha "única forma oficial", exemplo do YAML resultante), **Task 3** inspecionar (`config view` com `DATA+OMITTED`, `cluster-info`, `current-context`, padrão de nome `gke_<project>_<location>_<cluster>`, `get-contexts`, `use-context`, `source <(kubectl completion bash)` com explicação de process substitution e pegadinha "só vale na sessão atual"), **Task 4** deploy de Pods (a parte densa — explicado por que Pod fica em `Pending` no Autopilot via cluster-autoscaler scale-up, por que `kubectl top node` retorna "Metrics API not available" enquanto metrics-server popula, warning `autopilot-default-resources-mutator` injetando cpu=500m/memory=2Gi/ephemeral=1Gi via MutatingWebhook resultando em QoS Guaranteed, `describe pod` com seção Events narrando FailedScheduling → TriggeredScaleUp → Scheduled → Pulling → Pulled → Created → Started, `kubectl cp` colocando test.html em `/usr/share/nginx/html/` com anti-pattern marcado "não persiste em restart", `kubectl expose --type LoadBalancer` provisionando NLB externo via cloud-controller-manager + EXTERNAL-IP `<pending>` durante 30-90s, pegadinha "LoadBalancer só funciona em cloud integrado", explicação do Service `kubernetes` automático, validação `curl http://EXTERNAL_IP/test.html`, `kubectl top pods` mostrando uso ocioso 0m/2Mi vs request 500m), **Task 5** deploy declarativo via manifest YAML (`git clone training-data-analyst`, symlink `ak8s`, anatomia do `new-nginx-pod.yaml` com `apiVersion: v1`/`kind: Pod`/`metadata`/`spec`, `containerPort: 80` como mera documentação, `kubectl apply -f` 3-way merge idempotente vs `create` que quebra na 2ª vez, recap "Pod solto = armadilha sem self-healing nem rolling update — em prod use Deployment"). Tabelas finais: 7 cenários reais (PoC, onboarding, troubleshoot, validação, hotfix, expor, GitOps), cheatsheet com 12 comandos-chave, 6 armadilhas observadas com sintoma+causa+solução.
- [lab-cloud-build-quickstart.md](lab-cloud-build-quickstart.md) — **Lab Module 2 do curso GKE** (2026-05-18) — Cloud Build + Artifact Registry. 4 tasks documentadas: (1) habilitar APIs Cloud Build + Artifact Registry; (2) build "rápido" via `gcloud builds submit --tag` (Dockerfile alpine + quickstart.sh) com criação de repo Docker (`gcloud artifacts repositories create --repository-format=docker --location=REGION`); (3) build via `cloudbuild.yaml` com 1 step explícito (`gcr.io/cloud-builders/docker`, args + images); (4) build de 2 steps onde **a image recém-construída vira o executor do próximo step** com `args: ['fail']` simulando teste que falha, demonstrando fail-fast e propagação de exit code (`echo $?`). Conceitos: Artifact Registry (`pkg.dev`) substituindo Container Registry deprecado, `$PROJECT_ID` (substitution do Cloud Build) × `${DEVSHELL_PROJECT_ID}` (env var do Cloud Shell) — armadilha real, substitution variables built-in (`$BUILD_ID`, `$COMMIT_SHA`), Alpine + musl libc, campo `images:` obrigatório pra pushar. Tabela de 8 pegadinhas de prova. Cheatsheet completo de comandos + diagrama de fluxo Cloud Shell → Cloud Build → Artifact Registry.
- [lab-getting-started-gke-intro.md](lab-getting-started-gke-intro.md) — **Lab Module 1 do curso GKE** (2026-05-17). Walkthrough completo: Cloud Shell Editor (Theia/VS Code) editando `cleanup.sh` e criando `index.html`; `git clone` do repo `orchestrate-with-kubernetes`; `gcloud compute scp` do Cloud Shell pra VM `first-vm` (us-east1-c) com geração de chave SSH na 1ª execução; instalação de nginx via apt em VM Debian; cópia do `index.html` pro document root `/var/www/html`. **Troubleshooting documentado:** (a) `git clone` falhou em `/home` (não tem write access — fix: `cd ~`); (b) `gcloud compute scp` rodado por engano **dentro da VM** retornou `insufficient authentication scopes` (fix: rodar no Cloud Shell, não na VM); (c) browser mostrou ícone quebrado porque o HTML servia a URL **placeholder** do enunciado em vez da Public URL real do bucket Qwiklabs.

---

### 3.3 — Deploying and implementing Cloud Run and Cloud Run functions resources
**Workbook ACE — Diagnostic Questions:** 04, 05

**Tópicos principais:**
- **Cloud Run vs App Engine vs Cloud Run functions:**
  - Cloud Run — containers, pay-per-request, scale to zero
  - App Engine standard — sem container, runtimes fixos
  - App Engine flexible — containers em VMs, sem scale to zero
  - Cloud Run functions — código curto, event-driven
- **Triggers de Cloud Run functions com Cloud Storage:**
  - `google.storage.object.finalize` (objeto criado/finalizado)
  - outros eventos: delete, archive, metadataUpdate

**Arquivos documentados:**
- [3.3-cloud-run.md](3.3-cloud-run.md) — Cloud Run vs App Engine (Standard/Flexible) vs Cloud Run Functions, trigger `google.storage.object.finalize`, comparação de concorrência. DQs 04 e 05 resolvidas. **Curso "Developing Applications with Cloud Run: Fundamentals" (2026-05-19/20)** documentado end-to-end em 9 blocos: (1) Overview — services vs jobs, HTTPS automático em `*.run.app`, 7 formas de invocação (HTTPS/gRPC/WebSockets/Pub-Sub/Scheduler/Tasks/Eventarc), Array Jobs; (2) Resource Model — service + revisões + container instances, deploy via Console/gcloud/YAML/Terraform; (3) Container Lifecycle — 5 estados (Starting/Serving/Idle/ShuttingDown/Stopped), porta 8080, 100ms até idle, 15min em idle, SIGTERM com 10s, memory default 512MiB/max 32GiB, imagem puxada do Artifact Registry só no deploy e depois do storage interno (isolamento de falhas); (4) Autoscaling — scale to zero, cold start, min-instances (cobra!) × max-instances (default 100/hard 1000), concurrency (default 80/max 1000/min 1), CPU target 60%, cheat sheet de decisão; (5) Access Control — IAM policy/binding/role/permission, `allUsers + roles/run.invoker` = público, 3 ingress settings (All/Internal+LB/Internal), VPC SC contra data exfiltration, Serverless VPC Access Connector (/28, mesma região do service); (6) Service Identity — 4 tipos de identidades, service account (sem senha, email único, used by machines), default = Compute Engine SA com Editor (perigo), **diferença OAuth Access Token (Cloud Run → API Google) × OIDC ID Token (Cloud Run → Cloud Run)**, fluxos de auth completos, service-to-service síncrono com `roles/run.invoker`, async com Tasks/PubSub/Scheduler/Eventarc; (7) Resource Hierarchy — Organization > Folder > Project > Resource, herança de bindings unidirecional (top-down), effective policy = união de ancestrais + recurso, permissão herdada NÃO pode ser removida no filho (sobe na hierarquia ou usa Deny policy); (8) Principle of Least Privilege — 3 tipos de roles (Basic NUNCA em prod / Predefined default / Custom quando precisar), 3 passos para least privilege (nova SA → set as identity → bindings nos recursos específicos), SA nova começa sem permissões, comparativo Default vs User-managed; (9) Secrets and Env Vars — Secret Manager (metadados + secret versions), 2 formas de acessar secret (**volume mount com `latest` OK** vs **env var PINAR versão**), role `roles/secretmanager.secretAccessor`, env vars sobrescrevem default do Dockerfile ENV. **Quiz Módulo 1** com 5 questões corrigidas (pegadinhas: service revision é IMUTÁVEL, Cloud Run services são REGIONAIS, idle não cobra por default, CPU throttled impede background tasks). **Bloco 10 — Curso "Develop, Deploy, and Manage Applications on Cloud Run" — Module 3 inteiro (2026-05-20)** em 20 sub-blocos cobrindo as 3 lessons: **l1 Dev/Testing** (10.1 fit-check com 5 critérios de adequação, 10.2 dev workflow Docker-first × Source-based via Buildpacks, 10.3 container runtime contract — Linux 64-bit + escutar `$PORT` + SEM TLS no container + job exit code 0/non-zero, 10.4 execution environments 1st gen × 2nd gen com matriz de decisão e armadilha "jobs sempre 2nd gen", 10.5 file system in-memory tmpfs + Filestore/FUSE/client libs, 10.6 Cloud Code plugin VS Code/IntelliJ/Cloud Shell + nota sobre Cursor não oficialmente suportado, 10.7 local testing 3 caminhos Cloud Code/gcloud CLI/Docker); **l2 Deployments/Revisions** (10.8 build tools Docker × Cloud Build × Cloud Run source-based, 10.9 registry Artifact Registry recomendado + Container Registry deprecated + Cloud Run copia imagem internamente, 10.10 IAM pra deploy — Cloud Run Admin + SA User obrigatórios, 10.11 service configuration com **8 elementos que disparam revision** (image/entrypoint/secrets/timeout/concurrency/CPU-mem/scaling/SA-connector), 10.12 revision = imutável imagem+config, 10.13 serving traffic em 2 fases com zero-downtime, 10.14 pinning + tagging com URL `tag---svc.run.app` 3-hifens + splitting + session affinity); **l3 Integration** (10.15 client libraries usam SA automaticamente + armadilha default SA = Editor, 10.16 Memorystore Redis via Serverless VPC Connector mesma region + Cloud Run Integrations atalho UI/gcloud, 10.17 trigger Pub/Sub push subscription com ack deadline **600s** + idempotência obrigatória + HTTP codes 200/204=ack vs 4xx/5xx=retry, 10.18 Cloud SQL pub IP via Auth Proxy + priv IP via VPC connector, 10.19 best practices Secret Manager + connection pool limite **100 conexões/service**). **Lab GSP723 (2026-05-20)** documentado end-to-end na seção `## Labs`: Task 1-2 remoção de `--allow-unauthenticated` via `remove-iam-policy-binding` (armadilha: `--no-allow-unauthenticated` SÓ funciona em `deploy`, não em `update`); Task 3 redeploy com `--no-allow-unauthenticated` e validação curl → 403; Task 4 criação da SA `Billing Initiator` via Console + key file + `gcloud auth activate-service-account` + curl autenticado com `gcloud auth print-identity-token` retornando HTTP 204; Task 5 demonstrando que role no project = privilégio excessivo (SA invoca `billing-service-2` por herança), depois mover binding do project para `billing-service` específico, validando 204 no primeiro e 403 no segundo. **Lab GSP724 — Pub/Sub + Cloud Run (Critter Junction, 2026-05-20)** documentado end-to-end: cenário e-commerce com store-service público (producer, `--allow-unauthenticated`) + order-service privado (consumer, `--no-allow-unauthenticated`), Pub/Sub topic ORDER_PLACED com push subscription, decisão Pub/Sub × Cloud Tasks (Pub/Sub = distribuição genérica de eventos, Cloud Tasks = controle de timing de webhook específico), 6 tasks dissecadas: enable API, deploy producer + consumer, criação SA `pubsub-cloud-run-invoker` com **dois bindings críticos** (roles/run.invoker no order-service específico + **roles/iam.serviceAccountTokenCreator na SA gerenciada `service-PROJECT_NUMBER@gcp-sa-pubsub`** — armadilha de prova explicada — pra Pub/Sub conseguir gerar OIDC ID token em nome da SA), criação push subscription com `--push-auth-service-account`, teste end-to-end com curl JSON → store decodifica → publish → push delivery com Bearer token → order decodifica base64 → logs validam mesmo order_id atravessando os 2 services. Fluxo de 8 passos do request end-to-end documentado. Cenário real de microservices (frontend/cart/payment + analytics job) com SA dedicada por service. Tabela de **55+ pegadinhas de prova** consolidadas.

---

### 3.4 — Deploying and implementing data solutions
**Workbook ACE — Diagnostic Questions:** 06, 07, 08

**Tópicos principais:**
- **Buckets:** comandos `gcloud storage buckets create`, `gsutil mb`, escolha de location (regional / dual-region / multi-region)
- **Cloud SQL HA:** `gcloud sql instances create --availability-type=REGIONAL` para failover automático
- **Carregar dados no BigQuery:** Data Transfer Service vs `bq load` script vs streaming API vs Dataflow — escolher pelo custo/simplicidade

**Arquivos documentados:**
- [3.4-data-solutions.md](3.4-data-solutions.md) — Storage classes, location types, Cloud SQL HA (`--availability-type`), BigQuery ingestão (Data Transfer Service vs Streaming vs Dataflow). DQs 06, 07 e 08 resolvidas.

---

### 3.5 — Deploying and implementing networking resources
**Workbook ACE — Diagnostic Questions:** 09

**Tópicos principais:**
- **Modos de VPC:**
  - **Default** — criada com o projeto, subnets em todas as regiões
  - **Auto mode** — subnets criadas automaticamente em cada região com ranges fixos
  - **Custom mode** — você define tudo (IP ranges, subnets, regiões) → **controle total**
- Subnets regionais, IP ranges
- Firewall rules por tag/SA vs por IP
- IPs de VM (interno obrigatório, externo opcional, alias IPs)

**Arquivos documentados:**
- [3.5-networking-resources.md](3.5-networking-resources.md) — modos de VPC, firewall por tag, IPs de VM. Quiz "Virtual Networks" (2026-05-12) resolvido (Q1 ✅, Q2 ❌, Q3 ✅). **Cloud VPN:** Classic VPN (99.9% SLA, MTU 1460, site-to-site, 2 túneis, IKEv1/v2) vs HA VPN (99.99% SLA, 2 interfaces/IPs fixos, somente BGP, 2 ou 4 túneis, topologias: 2 peers / AWS / HA↔HA). Cloud Router e BGP (link-local 169.254.0.0/16, propagação automática de rotas). **Lab "Configuring Google Cloud HA VPN":** setup completo end-to-end (2 VPCs, 2 HA VPN gateways, 2 Cloud Routers com ASN, 4 túneis IKEv2, BGP peering por túnel com IPs 169.254.x.x/30, firewall cross-VPC, global routing mode, teste de failover com tunnel0 derrubado).
- [3.5-load-balancers.md](3.5-load-balancers.md) — **Curso "Elastic Google Cloud Infrastructure: Scaling and Automation"** (LB + autoscaling de MIGs). Blocos documentados: **(1) Introdução** — Cloud Load Balancing como serviço fully distributed/software-defined/managed (não é instance-based, single anycast IP global, 1M+ QPS); Application LB Layer 7 (HTTP(S) headers, cookies, URL paths, SSL/TLS termination, session affinity, content-based routing) vs Network LB Layer 4 (IP/porta, TCP/UDP, baixa latência, alta vazão, health checks). **(2) Managed Instance Groups** — definição (VMs idênticas via template), 4 superpoderes (rolling updates, autoscaling, LB integration, auto-healing), regional vs zonal (regional é o default recomendado para HA), fluxo de criação (template → MIG), 7 decisões na criação (stateless/stateful, nome, zona, port name mapping, template, autoscaling, health check), armadilhas (template imutável, auto-healing seletivo, health check duplo healing+LB). **(3) Autoscaling, Health Checks e Stateful IPs** — 4 categorias de policy (CPU / LB capacity / Monitoring metric / queue+schedule), modelo de média do grupo vs target, exemplo do slide 92.5% → +1 VM; 4 parâmetros do health check (check interval, timeout, healthy/unhealthy threshold) com cálculo do exemplo "2 falhas em 15s"; stateful IPs (IPv4 interno e externo, promoção ephemeral → static, diferença vs stateful MIG, cenários: licença por IP, lift-and-shift, peers hardcoded). **(4) Application LB — arquitetura detalhada** — Layer 7, backends suportados (CE, GKE, Cloud Storage, Cloud Run, external via internet/híbrido), External vs Internal; implementação GFE (Global/Classic, multi-region no Premium tier) vs Envoy (Global/Regional, advanced traffic management) e os 3 modos (Global/Classic/Regional); stack de 5 componentes (External Forwarding Rule → Target HTTP(S) Proxy + SSL certs → URL Map → Backend Service [health check, session affinity, timeout 30s fixed, lista de backends] → Backend [instance group + balancing mode + capacity scaler]); balancing mode (CPU vs RPS) × capacity scaler (multiplicador 0.0=drain, 1.0=cheio, 0.5=metade) com os 2 exemplos do vídeo esmiuçados; round-robin default vs session affinity; cross-region failover automático no Global; propagação de minutos. **(5) Decision tree — escolhendo o LB (fechamento da trilha)** — fluxo de 3 perguntas (tráfego → external/internal → global/regional) e tabela 3×4 com as ~6 variantes principais (Application/Proxy/Passthrough × External global/External regional/Internal regional/Cross-region); regra de uma frase por LB (Application = HTTP(S) flexível, Proxy NLB = TLS offload/TCP proxy/external multi-region, Passthrough NLB = preservar source IP, evitar overhead, UDP/ESP/ICMP, expor IP do cliente); load-balancing scheme como atributo da forwarding rule + backend service (`EXTERNAL_MANAGED`, `INTERNAL_MANAGED`, `EXTERNAL`, `INTERNAL`) e o que MANAGED significa (GFE ou Envoy interceptam; não-MANAGED = passthrough direto via Andromeda); Network Service Tiers (Premium obrigatório para LBs globais; Standard só regional); compilado final de armadilhas reunindo decision tree + MIG + autoscaling + Application LB stack. Armadilhas-chave: passthrough sempre regional, backend bucket só no Global external Application LB, preservar IP só com Passthrough, TLS offload em TCP não-HTTP só com Proxy NLB, `EXTERNAL_MANAGED` ≠ `EXTERNAL` (legacy).

---

### 3.6 — Implementing resources through infrastructure as code
**Workbook ACE — Diagnostic Questions:** 10

**Tópicos principais:**
- **Terraform — fluxo básico:**
  - `terraform init` — baixa providers
  - `terraform plan` — preview do que vai ser criado/mudado
  - `terraform apply` — efetivamente cria/atualiza recursos
  - `terraform destroy` — remove recursos
- Conceitos: providers, state, modules
- **Não está no exam guide oficial — opcional**

**Arquivos documentados:**
- [3.6-terraform.md](3.6-terraform.md) — Por que IaC, ferramentas (Terraform, Config Connector, Helm, Cloud Foundation Toolkit), lifecycle init→plan→apply→destroy. DQ 10 resolvida. **Curso "Elastic Google Cloud Infrastructure: Scaling and Automation":** bloco "Introdução — Automating the Deployment of GCP Infrastructure" (imperativo via Cloud API vs declarativo via Terraform, problema das dezenas de calls espalhadas, analogia post-its vs planta arquitetônica) + bloco "Terraform: a ferramenta, HCL e o fluxo init→plan→apply" (escada Console→CloudShell→Terraform, IaC benefícios, 5 ferramentas oficiais GCP — Terraform/Chef/Puppet/Ansible/Packer, paralelismo do Terraform vs sequencial do gcloud, sintaxe HCL com Blocks/Arguments/Expressions, anatomia do main.tf com provider/resource/output, exemplo guiado VPC auto-mode + firewall HTTP 80/8080, detalhes do init/plan/apply — plan faz refresh antes do diff).

---

## Como vamos documentar

Cada arquivo: conceito → por que → quando usar → armadilha de prova → exemplo `gcloud`/`kubectl`.
Erros de DQ → [../../flashcards.md](../../flashcards.md).
